<h3>Recent Posts</h3>
<% @posts.each do |post| %>
  <%= link_to post.title, post %>
  <%= content_tag :div, class: "content" do %>
    <small><%= post.created_at.strftime("%b %d, %Y") %></small>
    <br>
    <br>
  <% end %>
<% end %>